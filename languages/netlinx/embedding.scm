(
    (comment)* @context
    .
    (define_function
        (function_definition
        name: (_) @name)
    ) @item
)

(
    (comment)* @context
    .
    (struct_specifier
        name: (_) @name
    ) @item
)
